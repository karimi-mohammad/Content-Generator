version: '3.8'

services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      # Add your API keys here or use .env file
      # - GEMINI_API_KEY=your_key_here
      # - GOOGLE_API_KEY=your_key_here