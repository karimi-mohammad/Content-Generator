# Example .env for local development
# Please copy this file to .env and set your real keys (don't check .env into source control)

# Google Generative Language / Gemini API key
GEMINI_API_KEY="<your_google_gen_api_key_here>"
# Optionally: GEMINI_API_KEY or GOOGLE_API_KEY is used; default is GEMINI_API_KEY

# Optional: HTTP(S) proxy for outbound requests
HTTP_PROXY="http://127.0.0.1:12334"
HTTPS_PROXY="http://127.0.0.1:12334"
